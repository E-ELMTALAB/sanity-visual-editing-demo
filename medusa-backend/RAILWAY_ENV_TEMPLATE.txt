# =================================
# RAILWAY MEDUSA v2 ENVIRONMENT VARIABLES
# Copy these to your Railway Medusa service
# =================================

# Node Environment
NODE_ENV=production

# =================================
# REQUIRED - CORE CONFIGURATION
# =================================

# Database (automatically provided by Railway PostgreSQL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT Secret (REQUIRED - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_jwt_secret_here_min_32_chars_CHANGE_THIS

# Cookie Secret (REQUIRED - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
COOKIE_SECRET=your_cookie_secret_here_min_32_chars_CHANGE_THIS

# =================================
# OPTIONAL - BACKEND & CORS
# =================================

# Backend URL (auto-detected via RAILWAY_PUBLIC_DOMAIN_VALUE)
# BACKEND_PUBLIC_URL will be set automatically by Railway

# Store/Frontend CORS (your frontend URLs - comma-separated)
STORE_CORS=https://your-frontend.vercel.app,http://localhost:3000

# Admin CORS (comma-separated)
ADMIN_CORS=https://your-frontend.vercel.app,http://localhost:9000

# Auth CORS (comma-separated)  
AUTH_CORS=https://your-frontend.vercel.app,http://localhost:9000

# =================================
# OPTIONAL - REDIS (Recommended)
# =================================

# Redis (automatically provided by Railway Redis service)
REDIS_URL=${{Redis.REDIS_URL}}

# =================================
# OPTIONAL - MINIO FILE STORAGE
# =================================
# If not set, falls back to local file storage
# Use Railway MinIO template or external MinIO service

# MinIO Endpoint (without http://)
# MINIO_ENDPOINT=${{MinIO.RAILWAY_PRIVATE_DOMAIN}}:9000

# MinIO Access Key
# MINIO_ACCESS_KEY=your_minio_access_key

# MinIO Secret Key
# MINIO_SECRET_KEY=your_minio_secret_key

# MinIO Bucket (optional, defaults to 'medusa-media')
# MINIO_BUCKET=medusa-media

# =================================
# OPTIONAL - EMAIL NOTIFICATIONS
# =================================
# Choose ONE: Resend OR SendGrid (not both)

# Option 1: Resend (Recommended) - Get key from https://resend.com
# RESEND_API_KEY=re_your_resend_api_key
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# Option 2: SendGrid
# SENDGRID_API_KEY=SG.your_sendgrid_api_key
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# =================================
# OPTIONAL - PAYMENT PROVIDERS
# =================================

# Stripe Configuration - Get from https://stripe.com
# STRIPE_API_KEY=sk_test_your_stripe_test_key
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# =================================
# OPTIONAL - MEILISEARCH
# =================================
# Use Railway Meilisearch template

# Meilisearch Host
# MEILISEARCH_HOST=${{Meilisearch.RAILWAY_PRIVATE_DOMAIN}}:7700

# Meilisearch Admin API Key
# MEILISEARCH_ADMIN_KEY=${{Meilisearch.MEILI_MASTER_KEY}}

# =================================
# OPTIONAL - WORKER MODE
# =================================
# For scaling: set to 'server' on API instances, 'worker' on worker instances
# Default: 'shared' (runs both in same instance)

MEDUSA_WORKER_MODE=shared

# =================================
# OPTIONAL - DISABLE ADMIN
# =================================

# Set to 'true' if you want to disable the admin dashboard
MEDUSA_DISABLE_ADMIN=false

# =================================
# NOTES
# =================================
# 1. Generate secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 2. Railway service references use syntax: ${{ServiceName.VARIABLE_NAME}}
# 3. The backend URL is auto-detected from RAILWAY_PUBLIC_DOMAIN_VALUE
# 4. Make sure to add all your frontend domains to CORS variables
# 5. For production, always use production API keys (not test keys)
