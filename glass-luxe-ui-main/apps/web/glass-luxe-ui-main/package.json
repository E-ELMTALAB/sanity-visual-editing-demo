{
  "dependencies": {
    "@portabletext/react": "^5.0.0",
    "@sanity/visual-editing": "^4.0.3",
    "rxjs": "^7.8.2"
  },
  "scripts": {
    "build": "echo BUILD_SCRIPT_RUNNING && tsx ../scripts/fetch-homepage-data.ts && next build"
  }
}