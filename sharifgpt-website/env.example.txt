# ===========================================
# Cloudflare Proxy Configuration
# ===========================================
# Enable proxy mode to route requests through Cloudflare Workers
# Set to "true" to enable, anything else to disable
NEXT_PUBLIC_PROXY_ENABLED=true

# Option 1: Use a single unified worker that handles all services
# The unified worker routes requests based on path prefixes:
# - /cdn/* → Sanity CDN (images)
# - /api/* → Sanity API (GROQ queries)
# - /medusa/* → Medusa backend
NEXT_PUBLIC_UNIFIED_PROXY_URL=https://your-unified-proxy.workers.dev

# Option 2: Use separate workers for each service (if unified not set)
# Sanity CDN proxy for images
# NEXT_PUBLIC_SANITY_CDN_PROXY_URL=https://your-sanity-cdn-proxy.workers.dev

# Sanity API proxy for GROQ queries  
# NEXT_PUBLIC_SANITY_API_PROXY_URL=https://your-sanity-api-proxy.workers.dev

# Medusa backend proxy
# NEXT_PUBLIC_MEDUSA_PROXY_URL=https://your-medusa-proxy.workers.dev

# ===========================================
# Sanity Configuration
# ===========================================
NEXT_PUBLIC_SANITY_PROJECT_ID=zrvdkcjy
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2023-06-21
SANITY_API_READ_TOKEN=your-read-token

# ===========================================
# Medusa Configuration
# ===========================================
# Original Medusa backend URL (used when proxy is disabled)
NEXT_PUBLIC_MEDUSA_BACKEND_URL=https://backend.sharifgpt.com
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_2243c4f7a1f70eb2bb9b354ad7b22be869fca2633214edd7ee70637412a67bd4

# ===========================================
# Webhook Secrets
# ===========================================
SANITY_WEBHOOK_SECRET=your-webhook-secret

