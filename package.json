{
  "name": "sanity-visual-editing-workspace",
  "private": true,
  "workspaces": [
    "apps/studio",
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  }
}
